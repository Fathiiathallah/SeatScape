* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
}

body {
    background-color: #F5F5DC;
    color: #4B3621;
    height: 100vh;
    display: flex;
    flex-direction: column;
    overflow: hidden; /* Mengunci layar desktop agar tidak scroll */
}

/* NAVBAR - Dibuat lebih tipis */
.navbar {
    background-color: #D4C7B5;
    padding: 0.5vh 5%;
    flex-shrink: 0;
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
}

.main-logo { height: 30px; } /* Ukuran logo diperkecil */

.nav-right {
    display: flex;
    align-items: center;
    gap: 15px;
}

.search-bar {
    background: #F5F5DC;
    padding: 2px 10px;
    border-radius: 20px;
    display: flex;
    align-items: center;
}

.search-bar input {
    border: none;
    outline: none;
    background: transparent;
    width: 80px;
    font-size: 11px;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 12px;
}

.nav-links a {
    color: #4A3525;
    text-decoration: none;
    font-size: 12px;
}

/* HEADER - Padding dikurangi signifikan */
.thank-you-header {
    background-color: #F5F5DC;
    text-align: center;
    padding: 1vh 0;
    flex-shrink: 0;
}

.thank-you-header h1 {
    font-size: 1.8rem; /* Ukuran teks diperkecil */
    color: #4B3621;
    font-weight: 800;
}

/* MAIN CONTENT - Area cokelat utama */
.content-section {
    background-color: #4B3621;
    color: #F5F5DC;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 1.5vh 20px;
}

.main-title { 
    font-size: 2rem; 
    font-weight: 700;
    margin-bottom: 2px; 
}

.subtitle { 
    font-size: 0.8rem; 
    margin-bottom: 1.5vh; 
    opacity: 0.8; 
}

/* QR CONTAINER - Ukuran Barcode dioptimalkan */
.qr-container {
    background: white;
    padding: 10px;
    border-radius: 15px;
    display: inline-block;
    margin-bottom: 1.5vh;
}

.qr-code {
    height: 24vh; /* Tinggi barcode dikurangi agar ruang sisa cukup */
    width: auto;
    display: block;
}

.download-icon {
    margin-top: 8px;
    background: #8B735B;
    color: white;
    width: 30px;
    height: 30px;
    line-height: 30px;
    border-radius: 50%;
    margin: 8px auto 0;
    font-size: 0.8rem;
}

.info-text p { font-size: 0.8rem; }
.warning-text {
    font-size: 0.7rem;
    max-width: 500px;
    margin: 0.8vh auto 0;
    line-height: 1.3;
    font-weight: 600;
}

/* FOOTER - Dibuat sangat compact */
.footer {
    background-color: #D4C7B5;
    padding: 1vh 5%;
    flex-shrink: 0;
    color: #4A3525;
}

.footer-container {
    display: flex;
    justify-content: space-around;
    max-width: 900px;
    margin: 0 auto;
}

.footer-col h3 { font-size: 0.85rem; margin-bottom: 3px; font-weight: 700; }
.footer-col p { font-size: 0.75rem; margin-bottom: 2px; display: flex; align-items: center; gap: 6px; }

.copyright {
    text-align: center;
    margin-top: 0.5vh;
    font-size: 0.6rem;
    border-top: 1px solid rgba(245, 245, 220, 0.1);
    padding-top: 0.5vh;
    opacity: 0.7;
}

/* RESPONSIVE MOBILE - Tetap aman dan bisa scroll */
@media (max-width: 768px) {
    body { height: auto; overflow-y: auto; }
    .qr-code { height: auto; width: 180px; }
    .footer-container { flex-direction: column; text-align: left; gap: 15px; padding-left: 5%; }
}
